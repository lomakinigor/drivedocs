/// <reference types="vite/client" />

/** Короткий хэш коммита, подставляется на этапе сборки (vite.config.ts). */
declare const __APP_VERSION__: string
/** ISO-дата сборки, подставляется на этапе сборки (vite.config.ts). */
declare const __BUILD_DATE__: string
